import { SendTransactionError } from '@solana/web3.js';
export declare class CustomInstructionError extends Error {
    instructionIndex: number;
    code: number;
    constructor(instructionIndex: number, code: number);
    static parseSendTransactionError(error: SendTransactionError): CustomInstructionError;
}
