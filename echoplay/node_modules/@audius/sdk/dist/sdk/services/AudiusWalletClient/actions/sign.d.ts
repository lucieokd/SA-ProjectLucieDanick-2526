import type { Chain, Client, SignableMessage, Transport } from 'viem';
import type { AudiusAccount } from '../types';
export type SignParameters<TAccount extends AudiusAccount | undefined = AudiusAccount | undefined> = {
    account?: TAccount;
    message: SignableMessage;
};
export declare function sign<TChain extends Chain | undefined, TAccount extends AudiusAccount | undefined>(client: Client<Transport, TChain, TAccount>, { account: account_, message: message_ }: SignParameters): Promise<[Uint8Array, number]>;
