import type { Chain, Client, Transport } from 'viem';
import type { AudiusAccount } from '../types';
export type GetSharedSecretParameters<TAccount extends AudiusAccount | undefined = AudiusAccount | undefined> = {
    account?: TAccount;
    publicKey: string | Uint8Array;
};
export declare function getSharedSecret<TChain extends Chain | undefined, TAccount extends AudiusAccount | undefined>(client: Client<Transport, TChain, TAccount>, { account: account_, publicKey }: GetSharedSecretParameters<TAccount>): Promise<Uint8Array>;
