import { BaseAPI } from '../../api/generated/default';
type GetStemsArchiveJobStatusResponse = {
    id: string;
    state: 'completed' | 'failed' | 'active' | 'waiting' | 'delayed' | 'prioritized';
    progress?: number;
    failedReason?: string;
};
export declare class ArchiverService extends BaseAPI {
    createStemsArchive({ trackId, userId, includeParent }: {
        trackId: string;
        userId: string;
        includeParent?: boolean;
    }): Promise<GetStemsArchiveJobStatusResponse>;
    getStemsArchiveJobStatus({ jobId }: {
        jobId: string;
    }): Promise<GetStemsArchiveJobStatusResponse>;
    cancelStemsArchiveJob({ jobId }: {
        jobId: string;
    }): Promise<void>;
}
export {};
