import { type Hex } from 'viem';
import { type TransferTokensParams, type AudiusWormholeConfig } from './types';
export declare class AudiusWormholeClient {
    readonly contractAddress: Hex;
    private readonly audiusWalletClient;
    private readonly walletClient;
    private readonly publicClient;
    constructor(config: AudiusWormholeConfig);
    transferTokens(params: TransferTokensParams): Promise<`0x${string}`>;
    private domain;
}
