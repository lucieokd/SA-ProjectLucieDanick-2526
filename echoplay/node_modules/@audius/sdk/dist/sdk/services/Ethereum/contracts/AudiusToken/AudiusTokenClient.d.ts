import { type Hex } from 'viem';
import { type AudiusTokenConfig, type PermitParams, type BalanceOfParams } from './types';
export declare class AudiusTokenClient {
    readonly contractAddress: Hex;
    private readonly audiusWalletClient;
    private readonly walletClient;
    private readonly publicClient;
    constructor(config: AudiusTokenConfig);
    permit(params: PermitParams): Promise<`0x${string}`>;
    balanceOf(params: BalanceOfParams): Promise<bigint>;
    private domain;
}
