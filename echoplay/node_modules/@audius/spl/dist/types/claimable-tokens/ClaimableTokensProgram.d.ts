/// <reference types="node" />
import { PublicKey, TransactionInstruction } from '@solana/web3.js';
import { CreateClaimableTokensAccountParams, CreateClaimableTokensAccountInstructionData, DecodedCreateClaimableTokensAccountInstruction, TransferClaimableTokensUnsignedInstructionData, TransferClaimableTokensParams, DecodedTransferClaimableTokensInstruction, NonceAccountData, TransferClaimableTokensSignedInstructionData, DecodedClaimableTokenInstruction } from './types';
/**
 * The Claimable Tokens Program is responsible for the creation and control of
 * "user banks", which are accounts that are owned by the program itself but
 * controlled by users' Ethereum wallet addresses.
 *
 * Unlike normal Associated Token Accounts, the user bank accounts are owned
 * by the program, not a user's wallet. The only way for a user to transfer
 * tokens out of their user bank is by using this program method paired with
 * a signed Secp256k1 instruction from their Ethereum wallet specifying the
 * destination and amount.
 *
 * A user can have multiple user banks, one for each token mint.
 */
export declare class ClaimableTokensProgram {
    static readonly programId: PublicKey;
    static readonly layouts: {
        createAccountInstructionData: import("@solana/buffer-layout").Structure<CreateClaimableTokensAccountInstructionData>;
        unsignedTransferInstructionData: import("@solana/buffer-layout").Structure<TransferClaimableTokensUnsignedInstructionData>;
        signedTransferInstructionData: import("@solana/buffer-layout").Structure<TransferClaimableTokensSignedInstructionData>;
        nonceAccountData: import("@solana/buffer-layout").Structure<NonceAccountData>;
    };
    static createAccountInstruction({ ethAddress, payer, mint, authority, userBank, programId, tokenProgramId }: CreateClaimableTokensAccountParams): TransactionInstruction;
    static decodeCreateAccountInstruction({ programId, keys: [payer, mint, authority, userBank, rent, tokenProgramId, systemProgramId], data }: TransactionInstruction): DecodedCreateClaimableTokensAccountInstruction;
    static createTransferInstruction({ payer, sourceEthAddress, sourceUserBank, destination, nonceAccount, authority, programId, tokenProgramId }: TransferClaimableTokensParams): TransactionInstruction;
    static decodeTransferInstruction({ programId, keys: [payer, sourceUserBank, destination, nonceAccount, authority, rent, sysvarInstructions, systemProgramId, tokenProgramId], data }: TransactionInstruction): DecodedTransferClaimableTokensInstruction;
    static decodeInstruction(instruction: TransactionInstruction): DecodedClaimableTokenInstruction;
    static isCreateAccountInstruction(decoded: DecodedClaimableTokenInstruction): decoded is DecodedCreateClaimableTokensAccountInstruction;
    static isTransferInstruction(decoded: DecodedClaimableTokenInstruction): decoded is DecodedTransferClaimableTokensInstruction;
    static createSignedTransferInstructionData({ destination, amount, nonce }: TransferClaimableTokensSignedInstructionData): Buffer;
    static decodeSignedTransferInstructionData(instruction: TransactionInstruction): TransferClaimableTokensSignedInstructionData;
    static deriveNonce({ ethAddress: wallet, programId, authority }: {
        ethAddress: string;
        programId: PublicKey;
        authority: PublicKey;
    }): PublicKey;
    static deriveUserBank({ ethAddress: wallet, claimableTokensPDA, tokenProgramId }: {
        ethAddress: string;
        claimableTokensPDA: PublicKey;
        tokenProgramId?: PublicKey;
    }): Promise<PublicKey>;
    static deriveAuthority: ({ programId, mint }: {
        programId: PublicKey;
        mint: PublicKey;
    }) => PublicKey;
}
//# sourceMappingURL=ClaimableTokensProgram.d.ts.map