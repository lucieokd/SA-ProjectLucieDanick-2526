import { Layout } from '@solana/buffer-layout';
import { Attestation } from './types';
export declare class AttestationLayout extends Layout<Attestation> {
    constructor(property?: string);
    getSpan(b: Uint8Array, offset?: number): number;
    decode(b: Uint8Array, offset?: number): Attestation;
    encode(src: Attestation, b: Uint8Array, offset?: number): number;
}
export declare const attestationLayout: (property?: string) => AttestationLayout;
//# sourceMappingURL=AttestationLayout.d.ts.map